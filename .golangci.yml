version: "2"
run:
  allow-parallel-runners: true
linters:
  default: none
  enable:
    - copyloopvar
    - depguard
    - dupl
    - errcheck
    - ginkgolinter
    - gocyclo
    - govet
    - ineffassign
    - lll
    - modernize
    - misspell
    - nakedret
    - prealloc
    - revive
    - staticcheck
    - unconvert
    - unparam
    - unused
    - logcheck
  settings:
    custom:
      logcheck:
        type: "module"
        description: Checks Go logging calls for Kubernetes logging conventions.
    depguard:
      rules:
        forbid-sort-pkg:
          deny:
            - pkg: sort
              desc: Should be replaced with slices package
    revive:
      rules:
        - name: comment-spacings
        - name: import-shadowing
    modernize:
      disable:
        - omitzero
        - newexpr
  exclusions:
    generated: lax
    rules:
      - linters:
          - lll
        path: api/*
      - linters:
          - dupl
          - lll
        path: internal/*
      - linters:
          - dupl
          - lll
        path: bmc/*
      # The BMCUser resource is deprecated (see issue #1033). The operator still
      # serves and reconciles existing BMCUser objects, so these legacy consumers
      # reference the type until the resource is fully removed.
      - linters:
          - staticcheck
        text: "SA1019.*metalv1alpha1\\.BMCUser\\b"
      # The Endpoint resource is deprecated (see issue #1018). The operator still
      # serves existing Endpoint objects and the endpointRef access path, so these
      # legacy consumers reference the type until the resource is fully removed.
      - linters:
          - staticcheck
        text: "SA1019.*metalv1alpha1\\.Endpoint"
      # BMC.spec.endpointRef references the deprecated Endpoint resource. The operator
      # still serves existing BMCs that use it, so legacy consumers reference the field
      # until the Endpoint resource is fully removed.
      - linters:
          - staticcheck
        text: "SA1019.*\\.EndpointRef is deprecated"
      # Server.spec.power is deprecated. The field is an internal
      # actuator variable that controllers still write while traversing the state machine
      # and as surrogate input from ServerClaim. Legacy access remains
      # until the field is fully removed in a future release.
      - linters:
          - staticcheck
        text: "SA1019.*server\\.Spec\\.Power is deprecated"
    paths:
      - builtin$
      - examples$
formatters:
  enable:
    - gofmt
    - goimports
  exclusions:
    generated: lax
    paths:
      - builtin$
      - examples$

{{- if .Values.crd.enable }}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  labels:
    {{- include "chart.labels" . | nindent 4 }}
  annotations:
    {{- if .Values.crd.keep }}
    "helm.sh/resource-policy": keep
    {{- end }}
    controller-gen.kubebuilder.io/version: v0.18.0
  name: biossettingsflows.metal.ironcore.dev
spec:
  group: metal.ironcore.dev
  names:
    kind: BIOSSettingsFlow
    listKind: BIOSSettingsFlowList
    plural: biossettingsflows
    singular: biossettingsflow
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.version
      name: BIOSVersion
      type: string
    - jsonPath: .spec.serverRef.name
      name: ServerRef
      type: string
    - jsonPath: .status.state
      name: State
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: BIOSSettingsFlow is the Schema for the biossettingsflows API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BIOSSettingsFlowSpec defines the desired state of BIOSSettingsFlow.
            properties:
              serverMaintenancePolicy:
                description: ServerMaintenancePolicy is maintenance policy to be enforced
                  on the server.
                type: string
              serverRef:
                description: ServerRef is a reference to a specific server to apply
                  bios setting on.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
                x-kubernetes-validations:
                - message: serverRef is immutable
                  rule: self == oldSelf
              settingsFlow:
                description: SettingsFlow contains BIOS settings sequence to apply
                  on the BIOS in given order
                items:
                  properties:
                    priority:
                      description: |-
                        Priority defines the order of applying the settings
                        any int greater than 0. lower number have higher Priority (ie; lower number is applied first)
                      format: int32
                      maximum: 2147483645
                      minimum: 1
                      type: integer
                    settings:
                      additionalProperties:
                        type: string
                      type: object
                  required:
                  - priority
                  type: object
                type: array
                x-kubernetes-validations:
                - message: settingsFlow is immutable
                  rule: self == oldSelf
              version:
                description: 'Version contains software (eg: BIOS, BMC) version this
                  settings applies to'
                type: string
            required:
            - version
            type: object
          status:
            description: BIOSSettingsFlowStatus defines the observed state of BIOSSettingsFlow.
            properties:
              state:
                description: State represents the current state of the bios configuration
                  task.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
{{- end -}}

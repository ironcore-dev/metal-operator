---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: maintenancepipelineruns.metal.ironcore.dev
spec:
  group: metal.ironcore.dev
  names:
    kind: MaintenancePipelineRun
    listKind: MaintenancePipelineRunList
    plural: maintenancepipelineruns
    shortNames:
    - mpr
    singular: maintenancepipelinerun
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.bmcRef.name
      name: BMC
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.totalStages
      name: Stages
      type: integer
    - jsonPath: .status.activeStages
      name: Active
      type: integer
    - jsonPath: .status.completedStages
      name: Completed
      type: integer
    - jsonPath: .status.failedStages
      name: Failed
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          MaintenancePipelineRun is the Schema for the maintenancepipelineruns API.
          It is auto-generated by the MaintenancePipeline controller — one instance per unique BMC
          covered by the pipeline's serverSelector. It owns all BMCSettings, BMCVersion, BIOSSettings,
          and BIOSVersion child resources for both the BMC and all servers that share it.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              MaintenancePipelineRunSpec defines the desired state of MaintenancePipelineRun.
              MaintenancePipelineRun objects are auto-generated by the MaintenancePipeline controller —
              one per unique BMC covered by the pipeline's serverSelector.
            properties:
              bmcRef:
                description: |-
                  BMCRef references the BMC that this Run manages.
                  Immutable after creation.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
                x-kubernetes-validations:
                - message: bmcRef is immutable
                  rule: self == oldSelf
              serverRefs:
                description: |-
                  ServerRefs is the list of servers sharing this BMC that are targeted by the pipeline.
                  All servers must have spec.bmcRef pointing to the same BMC as bmcRef.
                items:
                  description: |-
                    LocalObjectReference contains enough information to let you locate the
                    referenced object inside the same namespace.
                  properties:
                    name:
                      default: ""
                      description: |-
                        Name of the referent.
                        This field is effectively required, but due to backwards compatibility is
                        allowed to be empty. Instances of this type with an empty value here are
                        almost certainly wrong.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      type: string
                  type: object
                  x-kubernetes-map-type: atomic
                minItems: 1
                type: array
            required:
            - bmcRef
            - serverRefs
            type: object
          status:
            description: MaintenancePipelineRunStatus defines the observed state of
              MaintenancePipelineRun.
            properties:
              activeStages:
                description: ActiveStages is the number of stages currently executing.
                format: int32
                type: integer
              completedStages:
                description: CompletedStages is the number of stages that have successfully
                  completed.
                format: int32
                type: integer
              conditions:
                description: Conditions represents the latest observations of the
                  Run.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              failedStages:
                description: FailedStages is the number of stages that have failed.
                format: int32
                type: integer
              pendingStages:
                description: PendingStages is the number of stages that have not yet
                  started.
                format: int32
                type: integer
              phase:
                description: Phase is the current aggregate phase of this Run.
                type: string
              stages:
                description: |-
                  Stages mirrors the owning pipeline's spec.stages list with per-stage progress tracking.
                  The controller populates this list when the Run is first reconciled.
                items:
                  description: MaintenancePipelineRunStageStatus is the observed status
                    of a single stage within a Run.
                  properties:
                    childRef:
                      description: |-
                        ChildRef references the child resource for BMC-scoped stages (BMCSettings, BMCVersion).
                        Nil for Server-scoped stages — see Servers[*].ChildRef instead.
                      properties:
                        kind:
                          description: |-
                            Kind is the kind of the child resource.
                            One of: BMCSettings, BMCVersion, BIOSSettings, BIOSVersion.
                          type: string
                        name:
                          description: Name is the generated name of the child resource.
                          type: string
                      required:
                      - kind
                      - name
                      type: object
                    completedAt:
                      description: CompletedAt is the time at which this stage reached
                        the Completed phase.
                      format: date-time
                      type: string
                    name:
                      description: Name identifies the stage; matches spec.stages[*].name
                        in the owning MaintenancePipeline.
                      type: string
                    phase:
                      description: |-
                        Phase is the aggregate phase for this stage.
                        For Server-scoped stages the aggregate reflects the slowest server; individual servers
                        advance to the next stage independently as soon as their own child completes.
                      type: string
                    servers:
                      description: |-
                        Servers tracks per-server status for BIOSSettings and BIOSVersion stages.
                        Empty for BMC-scoped stages.
                      items:
                        description: |-
                          MaintenancePipelineRunStageServerStatus tracks per-server progress for Server-scoped stages
                          (BIOSSettings and BIOSVersion).
                        properties:
                          childRef:
                            description: |-
                              ChildRef references the child resource created for this server.
                              Nil until the stage reaches this server.
                            properties:
                              kind:
                                description: |-
                                  Kind is the kind of the child resource.
                                  One of: BMCSettings, BMCVersion, BIOSSettings, BIOSVersion.
                                type: string
                              name:
                                description: Name is the generated name of the child
                                  resource.
                                type: string
                            required:
                            - kind
                            - name
                            type: object
                          phase:
                            description: Phase is the current phase for this server
                              within the stage.
                            type: string
                          serverRef:
                            description: ServerRef identifies the server this status
                              entry describes.
                            properties:
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                            type: object
                            x-kubernetes-map-type: atomic
                        required:
                        - phase
                        - serverRef
                        type: object
                      type: array
                  required:
                  - name
                  - phase
                  type: object
                type: array
              totalStages:
                description: TotalStages is the total number of stages defined in
                  the owning pipeline.
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}

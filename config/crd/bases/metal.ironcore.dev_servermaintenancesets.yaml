---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: servermaintenancesets.metal.ironcore.dev
spec:
  group: metal.ironcore.dev
  names:
    kind: ServerMaintenanceSet
    listKind: ServerMaintenanceSetList
    plural: servermaintenancesets
    singular: servermaintenanceset
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ServerMaintenanceSet is the Schema for the ServerMaintenanceSet
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ServerMaintenanceSetSpec defines the desired state of ServerMaintenanceSet.
            properties:
              serverLabelSelector:
                description: ServerLabelSelector specifies a label selector to identify
                  the servers that are to be maintained.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
              serverMaintenanceTemplate:
                description: Template specifies the template for the server maintenance.
                properties:
                  policy:
                    description: Policy specifies the maintenance policy to be enforced
                      on the server.
                    type: string
                  serverBootConfigurationTemplate:
                    description: ServerBootConfigurationTemplate specifies the boot
                      configuration to be applied to the server during maintenance.
                    properties:
                      name:
                        description: Name specifies the name of the boot configuration.
                        type: string
                      spec:
                        description: Parameters specify the parameters to be used
                          for rendering the boot configuration.
                        properties:
                          ignitionSecretRef:
                            description: |-
                              IgnitionSecretRef is a reference to the Kubernetes Secret object that contains
                              the ignition configuration for the server. This field is optional and can be omitted if not specified.
                            properties:
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                            type: object
                            x-kubernetes-map-type: atomic
                          image:
                            description: |-
                              Image specifies the boot image to be used for the server.
                              This field is optional and can be omitted if not specified.
                            type: string
                          serverRef:
                            description: ServerRef is a reference to the server for
                              which this boot configuration is intended.
                            properties:
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                            type: object
                            x-kubernetes-map-type: atomic
                        required:
                        - serverRef
                        type: object
                    required:
                    - name
                    - spec
                    type: object
                  serverPower:
                    description: ServerPower specifies the power state of the server
                      during maintenance.
                    type: string
                type: object
            required:
            - serverLabelSelector
            - serverMaintenanceTemplate
            type: object
          status:
            description: ServerMaintenanceSetStatus defines the observed state of
              ServerMaintenanceSet.
            properties:
              completed:
                description: Completed is the total number of completed server maintenances
                  in the set.
                format: int32
                type: integer
              failed:
                description: Failed is the total number of failed server maintenances
                  in the set.
                format: int32
                type: integer
              inMaintenance:
                description: InMaintenance is the total number of server maintenances
                  in the set that are currently in maintenance.
                format: int32
                type: integer
              maintenances:
                description: Maintenances is the number of server maintenances in
                  the set.
                format: int32
                type: integer
              pending:
                description: Pending is the total number of pending server maintenances
                  in the set.
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}

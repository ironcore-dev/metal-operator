{{- if .Values.crd.enable }}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  labels:
    {{- include "chart.labels" . | nindent 4 }}
  annotations:
    {{- if .Values.crd.keep }}
    "helm.sh/resource-policy": keep
    {{- end }}
    controller-gen.kubebuilder.io/version: v0.20.1
  name: biosversionsets.metal.ironcore.dev
spec:
  group: metal.ironcore.dev
  names:
    kind: BIOSVersionSet
    listKind: BIOSVersionSetList
    plural: biosversionsets
    shortNames:
    - biosvs
    singular: biosversionset
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.biosVersionTemplate.version
      name: BIOSVersion
      type: string
    - jsonPath: .status.fullyLabeledServers
      name: selectedServers
      type: integer
    - jsonPath: .status.availableBIOSVersion
      name: AvailableBIOSVersion
      type: integer
    - jsonPath: .status.pendingBIOSVersion
      name: Pending
      type: integer
    - jsonPath: .status.inProgressBIOSVersion
      name: InProgress
      type: integer
    - jsonPath: .status.completedBIOSVersion
      name: Completed
      type: integer
    - jsonPath: .status.failedBIOSVersion
      name: Failed
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: BIOSVersionSet is the Schema for the biosversionsets API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BIOSVersionSetSpec defines the desired state of BIOSVersionSet.
            properties:
              biosVersionTemplate:
                description: BIOSVersionTemplate defines the template for the BIOSVersion
                  resource to be applied to the servers.
                properties:
                  image:
                    description: Image specifies the image to use to upgrade to the
                      given BIOS version.
                    properties:
                      URI:
                        description: URI is the URI of the software image to install.
                        type: string
                      secretRef:
                        description: SecretRef is a reference to the Secret containing
                          the credentials to access the image URI.
                        properties:
                          name:
                            description: name is unique within a namespace to reference
                              a secret resource.
                            type: string
                          namespace:
                            description: namespace defines the space within which
                              the secret name must be unique.
                            type: string
                        type: object
                        x-kubernetes-map-type: atomic
                      transferProtocol:
                        description: TransferProtocol is the network protocol used
                          to retrieve the image URI.
                        type: string
                    required:
                    - URI
                    type: object
                  readinessGates:
                    description: ReadinessGates blocks this BIOSVersion in Pending
                      until all gates are satisfied.
                    items:
                      description: |-
                        ReadinessGate blocks a resource in Pending until the referenced object
                        satisfies the specified check.

                        Exactly one of Name or OwnedBy must be set (object resolution):
                          - Name: direct lookup — checks the exact named object.
                          - OwnedBy: set-child lookup — finds the child of Kind that is owned by
                            the named Set and associated with this BMC/Server via ownerReferences.

                        Exactly one of ConditionType or FieldMatch must be set (match criterion):
                          - ConditionType: checks that the named condition is set to True.
                          - FieldMatch: checks that a specific field equals the given value.
                      properties:
                        apiVersion:
                          description: APIVersion of the object whose condition is
                            checked, e.g. "metal.ironcore.dev/v1alpha1".
                          type: string
                        conditionType:
                          description: |-
                            ConditionType checks that the named condition on the referenced object is set to True.
                            Mutually exclusive with FieldMatch.
                          type: string
                        fieldMatch:
                          description: |-
                            FieldMatch checks that a specific field on the referenced object equals the given value.
                            Mutually exclusive with ConditionType.
                          properties:
                            fieldPath:
                              description: |-
                                FieldPath is the dot-notation path to the field on the referenced object,
                                e.g. ".status.state".
                              type: string
                            value:
                              description: Value is the expected string value of the
                                field.
                              type: string
                          required:
                          - fieldPath
                          - value
                          type: object
                        kind:
                          description: Kind of the object whose condition is checked,
                            e.g. "BMCSettings".
                          type: string
                        name:
                          description: |-
                            Name is the exact name of the object to look up.
                            Mutually exclusive with OwnedBy.
                          type: string
                        ownedBy:
                          description: |-
                            OwnedBy resolves the target by finding the child of Kind that is owned
                            by the named Set and associated with this BMC/Server via ownerReferences.
                            Mutually exclusive with Name.
                          properties:
                            kind:
                              description: Kind is the kind of the owning Set CRD,
                                e.g. "BMCSettingsSet".
                              type: string
                            localFieldPath:
                              description: |-
                                LocalFieldPath is the dot-notation path on the *current* resource whose
                                value is used as the match key, e.g. ".metadata.name" or ".spec.serverRef.name".
                                Must be set together with RemoteFieldPath.
                                When both are omitted the controller falls back to ownerReferences.
                              type: string
                            name:
                              description: Name is the name of the owning Set CRD.
                              type: string
                            remoteFieldPath:
                              description: |-
                                RemoteFieldPath is the dot-notation path on the *child* object to compare
                                against LocalFieldPath, e.g. ".spec.serverRef.name" or ".spec.bmcRef.name".
                                Must be set together with LocalFieldPath.
                              type: string
                          required:
                          - kind
                          - name
                          type: object
                          x-kubernetes-validations:
                          - message: localFieldPath and remoteFieldPath must both
                              be set or both be omitted
                            rule: has(self.localFieldPath) == has(self.remoteFieldPath)
                      required:
                      - apiVersion
                      - kind
                      type: object
                      x-kubernetes-validations:
                      - message: exactly one of name or ownedBy must be set
                        rule: has(self.name) != has(self.ownedBy)
                      - message: exactly one of conditionType or fieldMatch must be
                          set
                        rule: has(self.conditionType) != has(self.fieldMatch)
                    type: array
                  retryPolicy:
                    description: RetryPolicy defines the retry behavior for automatic
                      retries on transient failures.
                    properties:
                      maxAttempts:
                        description: |-
                          MaxAttempts is the maximum number of automatic retry attempts after failure.
                          0 means no automatic retries. Must be between 0 and 10 inclusive.
                          If not set, the operator-level default is used.
                        format: int32
                        maximum: 10
                        minimum: 0
                        type: integer
                    type: object
                  serverMaintenancePolicy:
                    description: ServerMaintenancePolicy is a maintenance policy to
                      be enforced on the server.
                    type: string
                  updatePolicy:
                    description: UpdatePolicy indicates whether the server's upgrade
                      service should bypass vendor update policies.
                    type: string
                  version:
                    description: Version specifies the BIOS version to upgrade to.
                    type: string
                required:
                - image
                - version
                type: object
              serverSelector:
                description: ServerSelector specifies a label selector to identify
                  the servers that are to be selected.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
            required:
            - serverSelector
            type: object
          status:
            description: BIOSVersionSetStatus defines the observed state of BIOSVersionSet.
            properties:
              availableBIOSVersion:
                description: AvailableBIOSVersion is the number of BIOSVersion created
                  by the set.
                format: int32
                type: integer
              completedBIOSVersion:
                description: CompletedBIOSVersion is the total number of completed
                  BIOSVersion in the set.
                format: int32
                type: integer
              failedBIOSVersion:
                description: FailedBIOSVersion is the total number of failed BIOSVersion
                  in the set.
                format: int32
                type: integer
              fullyLabeledServers:
                description: FullyLabeledServers is the number of servers in the set.
                format: int32
                type: integer
              inProgressBIOSVersion:
                description: InProgressBIOSVersion is the total number of BIOSVersion
                  resources in the set that are currently in progress.
                format: int32
                type: integer
              pendingBIOSVersion:
                description: PendingBIOSVersion is the total number of pending BIOSVersion
                  in the set.
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
{{- end -}}
